name: walkthrough

on: [create, push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Test
        id: test
        uses: mscwilson/changelog-test/issue_labels@v2
        with:
          number: ${ GITHUB_REF }
